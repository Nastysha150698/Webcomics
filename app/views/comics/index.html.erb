

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @comics.each do |comic| %>
      <tr>
        <td><%= comic.name %></td>
        <td><%= link_to 'Show', comic_path(comic) %></td>
        <td><%= link_to 'Edit', edit_comic_path(comic) %></td>
        <td><%= link_to 'Destroy', comic_path(:id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<div class="comicPreviews">
  <div class="logo"></div>
  <div class="comicPreviewsHeaders">
    <h1>Simple and fast way to create animated web-comics</h1>
    <h2>Version 0.0.1. You can create and even edit objects</h2>
  </div>

  <a class="comicPreviewNewComic"href="<%= new_comic_path %>">
    <%= image_tag("plus.svg") %>
    Create comic
  </a>

  <% @comics.each do |comic| %>
    <%= render "comics/comicPreview", comic: comic %>
  <% end %>
</div>


<%= link_to 'New Comic', new_comic_path %>
