<%= form_with(model: [@comic, @font]) do |form| %>
  <% if @font.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@font.errors.count, "error") %> prohibited this font from being saved:</h2>

      <ul>
      <% @font.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="Sidebar">
    <div class="formName">
      Adding font
    </div>

    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title %>
      ex. Amatic SC
    </div>


    <div class="field">
      <%= form.label :link %>
      <%= form.text_field :link %>
      ex. https://fonts.googleapis.com/css?family=Amatic+SC
    </div>


    <div class="actions">
      <%= form.submit %>
    </div>
  </div>
<% end %>
