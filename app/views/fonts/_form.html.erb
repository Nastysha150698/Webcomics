<%= form_with(model: font, local: true) do |form| %>
  <% if font.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(font.errors.count, "error") %> prohibited this font from being saved:</h2>

      <ul>
      <% font.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <p>ex. Amatic SC</p>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <p>ex. https://fonts.googleapis.com/css?family=Amatic+SC</p>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
