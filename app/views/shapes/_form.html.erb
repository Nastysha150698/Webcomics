<%= form_with(model: shape, local: true) do |shape| %>
   <% if shape.errors.any? %>
     <div id="error_explanation">
       <h2><%= pluralize(shape.errors.count, "error") %> prohibited this shape from being saved:</h2>

       <ul>
       <% shape.errors.full_messages.each do |message| %>
         <li><%= message %></li>
       <% end %>
       </ul>
     </div>
   <% end %>

   <div class="field">
     <%= form.label :title %>
     <%= form.text_field :title %>
   </div>

   <p>?</p>

   <div class="field">
     <%= form.label :link %>
     <%= form.text_field :link %>
   </div>

   <p>?</p>

   <div class="actions">
     <%= form.submit %>
   </div>
 <% end %>
